version: '3.6'
services:
  applicative:
    build:
      context: .
      target: dev
    volumes:
      - modules:/opt/boiler/node_modules
      - type: bind
        source: .
        target: /opt/app/

volumes:
  modules:
    driver: local
